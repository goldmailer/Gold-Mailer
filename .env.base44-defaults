# Base44 dev defaults — placeholders so the app boots without real credentials.
# Anything in /run/base44/app.env (platform secrets) overrides these.
ADMIN_EMAIL=admin@goldmailer.local
ADMIN_PASSWORD=goldmailer-dev-admin-2026
